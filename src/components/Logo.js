import logo from "../assets/logo.svg"

const Logo = () => {
    return (
        <div>
            <img src={logo} alt="a logo for sunny side landing page"/>
        </div>
    )
}

export default Logo;
